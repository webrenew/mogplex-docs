---
title: Authentication
description: Understand Mogplex auth precedence, browser sign-in, stored provider keys, and what `mogplex login`, `/login`, and `/logout` do.
---

Mogplex can run with either direct provider credentials or your Mogplex account.
When a session starts, it resolves auth in this order.

## Resolution order

1. **Environment variable first.** If a provider env var is set in your shell,
   Mogplex uses it.

   | Provider   | Environment variable             |
   | ---------- | -------------------------------- |
   | Mogplex    | `MOGPLEX_API_KEY`                |
   | Anthropic  | `ANTHROPIC_API_KEY`              |
   | OpenAI     | `OPENAI_API_KEY`                 |
   | Google     | `GOOGLE_GENERATIVE_AI_API_KEY`   |
   | Groq       | `GROQ_API_KEY`                   |
   | Mistral    | `MISTRAL_API_KEY`                |
   | DeepSeek   | `DEEPSEEK_API_KEY`               |
   | xAI        | `XAI_API_KEY`                    |
   | Cohere     | `COHERE_API_KEY`                 |
   | Vercel     | `VERCEL_API_TOKEN`               |

2. **Fallback to stored credentials.** If the env var is not set, Mogplex reads
   `~/.mogplex/auth.json`.

3. **Stored Mogplex account login.** If the only stored credential is your
   Mogplex browser login, the CLI starts against your account-backed model
   access and synced catalog.

4. **No credentials → startup prompt or clear error.** In interactive TTY startup,
   bare `mogplex` prompts you to either sign in with Mogplex or store a provider
   API key. In non-interactive contexts it exits with a clear message telling
   you to run `mogplex`.

## What "account login" really means

Browser login is not the same thing as storing a direct provider key locally.

When you log in with your Mogplex account, the CLI reuses the hosted account's:

- model access
- synced model catalog
- synced MCP configuration
- Mogplex-managed authentication path

That is the cleanest setup when you want the web app and CLI to share
configuration.

## Mogplex account login

Running `mogplex` and choosing **Sign in with Mogplex** opens the webapp login
flow and stores a `mogplex` token in `~/.mogplex/auth.json`.

The browser flow lands on `/cli-auth`, validates the localhost callback the CLI
opened, and asks the signed-in user to approve the token handoff.

That login now does three things for the CLI:

- unlocks your synced model catalog
- unlocks synced MCP configuration
- runs prompts through the model access configured for your Mogplex account

If your account does not have model access yet, add an AI Gateway, OpenAI, or
Anthropic key in **Settings > API Keys**.

## `mogplex login` and `/login`

Use `mogplex` as the normal entrypoint:

```bash
mogplex
```

If you prefer to jump straight into the auth flow, `mogplex login` opens the
same chooser.

Inside the TUI, `/login` still works for storing direct provider keys when you
want the CLI to use a specific provider credential locally.

## Provider keys

Direct provider credentials still work and still take precedence over the
account login path. This is useful for shell-driven setups and CI.

```bash
export OPENAI_API_KEY=sk-...
mogplex
```

This is the preferred pattern for CI, ephemeral shells, and environments where
you do not want credentials written to `~/.mogplex/auth.json`.

## `/logout`

Sign-out is currently exposed through the slash surface, not through a wired
standalone `mogplex logout` subcommand.

Inside a live session, `/logout` removes the stored credential from
`~/.mogplex/auth.json`.

Two things to know:

- **If the matching env var is set, the next session still authenticates via the
  env var.** This is by design. Env vars are the source of truth for shell-driven
  setups.
- **If you log out of Mogplex, synced models and MCP entries disappear on the
  next startup.**

## Common outcomes and what they mean

- `mogplex login status` shows only a Mogplex credential: the CLI is in
  account-backed mode.
- `mogplex login status` shows a provider credential and the session uses that
  provider: direct provider auth is active.
- the CLI behaves differently than `login status` suggests: check env vars first
  because they outrank stored credentials.
- account login succeeds but prompts fail: the hosted account probably still
  lacks usable model access in [Web Settings](/web/settings).

## Logging and credential safety

Session logs at `~/.mogplex/logs/<session-id>.jsonl` never contain:

- API keys or OAuth tokens
- Contents of `~/.mogplex/auth.json`

All data is redacted before it is written to disk.

When `MOGPLEX_DEBUG=1` is set, debug logs also capture stream chunks and tool
outputs. These may include prompt or tool content, so treat debug logs with the
same care as your source code.
