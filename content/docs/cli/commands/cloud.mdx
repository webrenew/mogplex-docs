---
title: cloud
description: Browse Codex Cloud tasks (experimental).
---

{/* Auto-generated by scripts/gen-cli-reference.ts — do not edit by hand. */}

> **Experimental**: This command is experimental and may change.

## Status

Help-surface-only experimental command name.

## Synopsis

```bash
mogplex cloud [OPTIONS] [ARGS]
```

## Description

Browse Codex Cloud tasks (experimental).

## Current Implementation Status

The command name is present in the CLI help surface, but the standalone `mogplex cloud` path is not wired yet in the current build.
At the moment, running it can return `Subcommand 'cloud' is not yet implemented.`

## What To Use Today

- Use the [web app](/web) and [Observability](/web/observability) for hosted activity today.

## Why This Page Exists

The docs intentionally track the names exposed by `mogplex --help`, even when a standalone command is still reserved or partial.
That makes it clear which names are already part of the public help surface and which ones are not yet production-ready entrypoints.

## Aliases

- `cloud-tasks`

## Options

Run `mogplex cloud --help` to see all supported flags.

## See also

- [Web App Overview](/web)
- [Observability](/web/observability)
- [All commands](/cli/commands)
