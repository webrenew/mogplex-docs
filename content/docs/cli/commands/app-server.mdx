---
title: app-server
description: Run the JSON-RPC app-server (experimental).
---

{/* Auto-generated by scripts/gen-cli-reference.ts — do not edit by hand. */}

> **Experimental**: This command is experimental and may change.

## Status

Help-surface-only experimental command name.

## Synopsis

```bash
mogplex app-server [OPTIONS] [ARGS]
```

## Description

Run the JSON-RPC app-server (experimental).

## Current Implementation Status

The command name is present in the CLI help surface, but the standalone `mogplex app-server` path is not wired yet in the current build.
At the moment, running it can return `Subcommand 'app-server' is not yet implemented.`

## What To Use Today

- This is an experimental help-surface entry.
- Do not build automation around it yet.

## Why This Page Exists

The docs intentionally track the names exposed by `mogplex --help`, even when a standalone command is still reserved or partial.
That makes it clear which names are already part of the public help surface and which ones are not yet production-ready entrypoints.

## Options

Run `mogplex app-server --help` to see all supported flags.

## See also

- [All commands](/cli/commands)
