---
title: mcp-server
description: Run mogplex as an MCP server on stdio.
---

{/* Auto-generated by scripts/gen-cli-reference.ts — do not edit by hand. */}

## Status

Help-surface-only reserved command name.

## Synopsis

```bash
mogplex mcp-server [OPTIONS] [ARGS]
```

## Description

Run mogplex as an MCP server on stdio.

## Current Implementation Status

The command name is present in the CLI help surface, but the standalone `mogplex mcp-server` path is not wired yet in the current build.
At the moment, running it can return `Subcommand 'mcp-server' is not yet implemented.`

## What To Use Today

- Use [Settings](/web/settings) for shared MCP configuration.
- Use `mogplex exec "/mcp"` for the current local inspection path.

## Why This Page Exists

The docs intentionally track the names exposed by `mogplex --help`, even when a standalone command is still reserved or partial.
That makes it clear which names are already part of the public help surface and which ones are not yet production-ready entrypoints.

## Options

Run `mogplex mcp-server --help` to see all supported flags.

## See also

- [Web Settings](/web/settings)
- [All commands](/cli/commands)
