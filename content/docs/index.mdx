---
title: Get started
description: Start with the right Mogplex surface, follow the first-day setup path, and get to a working repo quickly.
---

Mogplex is easiest to understand as one product with two working surfaces:

- the **web app** is the shared control plane for GitHub coverage, repo setup,
  reusable agents, routing, settings, and runtime inspection
- the **CLI** is the terminal-native command center for supervising AI coding
  runs locally — agents, runs, MCP, memory, diffs, approvals, cost, and model
  routing on one screen

You do not need to understand the whole product before you get value from it.

The first useful question is simpler:

**Where does this piece of work start?**

<Cards>
  <Card
    title="CLI"
    href="/cli"
    description="Terminal cockpit for runs, agents, approvals, diffs, cost, and model routing — with attach mode for in-flight runs."
  />
  <Card
    title="Web app"
    href="/web"
    description="Projects, sandboxes, agents, automations, triggers, observability, settings, installations, and the hosted API."
  />
  <Card
    title="Extend"
    href="/extend"
    description="Reach Mogplex from outside Mogplex — Skills today, MCP server in preview."
  />
</Cards>

In practice, most users do this:

1. set up shared account state once in the web app
2. do repo-in-place work in the CLI
3. return to the web app when they need routing, inspection, or repair actions

## Start in the web app when...

- Mogplex still needs to connect GitHub, install the GitHub App, or sync repos.
- You need reusable agents, hosted connections, model access, or shared MCP
  definitions.
- The work should run from GitHub events, repo assignments, schedules, or a
  publishable workflow.
- You are debugging why routing, coverage, or hosted execution looks wrong.

## Start in the CLI when...

- The repo is already checked out locally.
- You want to watch a run end-to-end without leaving the terminal.
- You need to approve or reject tool calls as they happen.
- You want to attach to an in-flight run that started elsewhere
  (`mogplex --attach <runId>`).

## Fast decision guide

- “I need Mogplex to see my repos and respond to GitHub events.”
  Start with [Web App](/web), especially [Settings](/web/settings),
  [Installations](/web/installations), and [Projects](/web/spaces).
- “I already have the repo open locally and want to drive Mogplex from the terminal.”
  Start with [CLI](/cli) and run `mogplex`.
- “I need one GitHub event to wake one agent.”
  Start with [Triggers](/web/triggers).
- “I need standing work owned by one repo, like PR review or cron.”
  Start with [Assignments](/web/assignments).
- “I need branching, delays, parallel work, or a draft/publish workflow.”
  Start with [Automations](/web/automations).

## The shortest reliable first-day path

If you want one setup order that works for most accounts and repos, use this:

1. Open the [web app](/web) and connect GitHub in [Settings](/web/settings).
2. Confirm the correct user or org has Mogplex GitHub App coverage in
   [Installations](/web/installations).
3. Sync repos and open the target repo in [Projects](/web/spaces).
4. Check or create the reusable agents you want in [Agents](/web/agents).
5. Choose the smallest routing surface that fits the job:
   - [Triggers](/web/triggers) for `one event -> one agent`
   - [Assignments](/web/assignments) for standing repo-owned work
   - [Automations](/web/automations) for multi-step workflows
6. Install the [CLI](/cli) and run a first local task in the repo.
7. Use [Observability](/web/observability) once work is actually running.

## The fastest end-to-end proof

If you only want to prove Mogplex works before you invest in deeper routing
setup, install the CLI and start a run:

```bash
curl -fsSL https://www.mogplex.com/install.sh | sh
mogplex
```

The CLI opens the in-app login screen on first run. Sign in with Mogplex or
store a provider key locally; environment variables exist as escape hatches
only and are not required for normal use.

That first proof is credible when:

- `mogplex` opens the cockpit and the Header shows `transport: nominal`
- `/run summarize this repository` produces agents in the Agents panel and
  events in the Timeline
- the same account appears correctly in [Web Settings](/web/settings)

## What “working” should mean after the first hour

By the end of an initial pass, you should be able to say yes to these:

- Mogplex can see the repos you expect.
- The repo owner has GitHub App coverage, not just OAuth visibility.
- At least one reusable agent is configured the way you expect.
- One local CLI run completes successfully.
- You know whether this job belongs in Triggers, Assignments, or Automations.

If one of those is still false, the best next pages are usually:

- [Web App Overview](/web) if the issue is setup, routing, or hosted state
- [CLI Overview](/cli) if the issue is local execution or repo-aware behavior
- [GitHub Mention Routing](/web/guides/github-mention-routing) if GitHub
  comment routing is not behaving the way you expect
- [Choose the Right Routing Surface](/web/guides/choose-routing-surface) if the
  job still does not obviously belong in Triggers, Assignments, or Automations
